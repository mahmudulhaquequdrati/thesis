# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit the API key
.env

# Generated data. The SQLite file is the scientific asset: back it up
# separately (it costs real money to regenerate), but keep it out of git.
data/*.sqlite
data/figures/
# Regenerated from the database by scripts/make_viewer.py -- never hand-edited,
# and it inlines the whole dataset, so it does not belong in git either.
data/viewer.html
